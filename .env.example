# WebSocket Configuration
# Set to 'false' to disable WebSocket (for Vercel deployment)
VITE_WS_ENABLED=true

# WebSocket URL (only used when WS_ENABLED is true)
VITE_WS_URL=ws://localhost:8080

# Production deployment notes:
# - Vercel: WebSocket will be automatically disabled
# - Local dev: WebSocket will be enabled by default
# - Same-tab sync works everywhere via BroadcastChannel
